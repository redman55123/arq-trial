# AGENTS.md

## Cursor Cloud specific instructions

This repository (`arq-trial`) is currently an empty/blank-slate project initialized with only a `README.md`. There are no source files, dependencies, build systems, or services to run.

- **No package manager lockfile or dependency manifest** exists yet (`package.json`, `requirements.txt`, `Cargo.toml`, etc.).
- **No build/lint/test commands** are available.
- **No services or applications** to start.

When code is added to this repository, update this file with relevant development environment instructions (how to install dependencies, run lint/tests/build, and start the application).
